# Sanitized planning request

Improve a multi-agent workflow so each implementation slice produces a usable
maintainer outcome and remains traceable to the product contract.

This fixture contains no credentials, personal data, or external paths.
